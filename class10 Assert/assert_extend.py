'''
目标断言扩展：
    两种实现方式：
        1.使用unittest框架中的断言
        2.使用python 中自带断言
'''

# 1 使用python 自带断言判断两个字符是否相等
# 相等
# assert 'hello' == 'hello'
# 不相等
# assert 'hello' == 'hello1' 'not equal!!!'

# 2 第二个字符串是否包含第一个字符串
# 包含
# assert 'h' in 'hello'
# 不包含
# assert 'w' in 'hello'

# 3 assert True or assert 1  #True
# assert False or assert 0 #False

